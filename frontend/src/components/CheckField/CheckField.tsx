import React, { ReactElement } from 'react';

interface Props {}

function CheckField({}: Props): ReactElement {
  return <div>{/* Show a form check field */}</div>;
}

export default CheckField;

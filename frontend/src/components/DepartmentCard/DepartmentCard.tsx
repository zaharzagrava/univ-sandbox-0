import React, { ReactElement } from 'react';

interface Props {}

function DepartmentCard({}: Props): ReactElement {
  return <div>{/* Show a department card */}</div>;
}

export default DepartmentCard;

import React, { ReactElement } from 'react';

interface Props {}

function ClientList({}: Props): ReactElement {
  return <div>{/* Show a list of clients */}</div>;
}

export default ClientList;

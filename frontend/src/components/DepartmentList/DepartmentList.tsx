import React, { ReactElement } from 'react';

interface Props {}

function DepartmentList({}: Props): ReactElement {
  return <div>{/* Show a list of departments */}</div>;
}

export default DepartmentList;

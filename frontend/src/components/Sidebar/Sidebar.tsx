import React, { ReactElement } from 'react';

interface Props {}

function Sidebar({}: Props): ReactElement {
  return <div>{/* Show sidebar with options */}</div>;
}

export default Sidebar;

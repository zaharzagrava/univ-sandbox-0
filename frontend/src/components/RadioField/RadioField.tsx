import React, { ReactElement } from 'react';

interface Props {}

function RadioField({}: Props): ReactElement {
  return <div>{/* Show radio form field */}</div>;
}

export default RadioField;

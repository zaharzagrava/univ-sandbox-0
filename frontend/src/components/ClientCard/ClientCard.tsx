import React, { ReactElement } from 'react';

interface Props {}

function ClientCard({}: Props): ReactElement {
  return <div>{/* Show a client card */}</div>;
}

export default ClientCard;

import React, { ReactElement } from 'react';

interface Props {}

function RegisterCard({}: Props): ReactElement {
  return <div>{/* Show register form */}</div>;
}

export default RegisterCard;

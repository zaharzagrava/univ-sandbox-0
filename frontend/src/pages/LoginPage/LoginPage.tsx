import React, { ReactElement } from 'react'

interface Props {
  
}

function LoginPage({}: Props): ReactElement {
  return (
    <div>
      {/* Show a login page */}
    </div>
  )
}

export default LoginPage

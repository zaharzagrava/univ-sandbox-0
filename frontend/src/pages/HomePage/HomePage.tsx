import React, { ReactElement } from 'react'

interface Props {
  
}

function HomePage({}: Props): ReactElement {
  return (
    <div>
      {/* Show a home page */}
    </div>
  )
}

export default HomePage

import React, { ReactElement } from 'react'

interface Props {
  
}

function AdminPage({}: Props): ReactElement {
  return (
    <div>
      {/* Show an admin page */}
    </div>
  )
}

export default AdminPage
